name: 构建Docker镜像

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 构建Docker镜像
        run: docker build -t my-nginx .

      - name: 验证镜像
        run: docker run --rm my-nginx nginx -t
